# goreleaser.yml
# Build customization
project_name: rsq

release:
  prerelease: true

build:
  binary: rsq
  goos:
    - windows
    - darwin
    - linux
  goarch:
    - amd64

# goreleaser.yml
brew:
  # Repository to push the tap to.
  github:
    owner: metrumresearchgroup 
    name: homebrew-tap 
